<UserControl x:Class="GuildManager.Client.View.Controls.DialogueChoiceButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Button Click="Button_Click" Background="Transparent" BorderThickness="0"
            Cursor="Hand" FocusVisualStyle="{x:Null}">
        <Button.Template>
            <ControlTemplate TargetType="Button"><ContentPresenter/></ControlTemplate>
        </Button.Template>
        <Grid>
            <Image x:Name="ButtonImage" Source="/Assets/UI/choice_box.png" Stretch="Fill"/>
            <TextBlock Text="{Binding Text, ElementName=Root}" Foreground="#3B2412" FontSize="16"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Button>
</UserControl>